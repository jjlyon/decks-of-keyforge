---basic
install java-17, yarn, npm, 
gradlew (and other scripts) needs to be executable

---backend
./gradlew genSrc before bootRun
doesn't run postgresql
docker pull postgres
docker run --name keyswapdb -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres
wrong password...
make keyswap db
setup s3 and user key/secret access
setup patreon key (and others, bogus is fine)

---frontend
./gradlew genTs # maybe
cd ui
yarn
npm run start